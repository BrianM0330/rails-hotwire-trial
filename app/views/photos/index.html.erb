<% content_for :title, "All Photos - Clever Real Estate" %>

<div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
  <div class="mb-8 flex flex-col gap-3 border-b border-gray-100 pb-6 transition-colors duration-200 dark:border-zinc-800 sm:flex-row sm:items-end sm:justify-between">
    <div>
      <p class="text-sm font-semibold uppercase tracking-[0.2em] text-clever-600 dark:text-clever-400">Private Gallery</p>
      <h1 class="mt-2 text-3xl font-bold tracking-tight text-gray-900 dark:text-white">All Photos</h1>
      <p class="mt-2 text-sm text-gray-500 dark:text-zinc-400">All 10 seeded photos from the provided CSV.</p>
    </div>

    <div class="text-sm font-medium text-gray-500 dark:text-zinc-400">
      <%= pluralize(@photos.size, "photo") %>
    </div>
  </div>

  <% if @photos.any? %>
    <div class="grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4" id="photos" data-controller="gallery-lightbox">
      <%= render partial: "photo", collection: @photos, as: :photo, locals: { liked_photo_ids: @liked_photo_ids } %>
    </div>
  <% else %>
    <div class="rounded-2xl border border-dashed border-gray-200 bg-white p-10 text-center shadow-sm dark:border-zinc-800 dark:bg-zinc-900">
      <h2 class="text-lg font-semibold text-gray-900 dark:text-white">No photos yet</h2>
      <p class="mt-2 text-sm text-gray-500 dark:text-zinc-400">Seed the database from the CSV to populate this gallery.</p>
    </div>
  <% end %>
</div>
